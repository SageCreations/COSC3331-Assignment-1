public class arraySet {
    int[] set;
    int nElems;

    public arraySet(int maxSize) {
        set = new int[maxSize];
        nElems = 0;
    }

    public void PopulateArray() {
        //TODO: randomly populate the arraySet with inclusive range of random values using [1, arraySize * 2]

        //TODO: might be able to just use this function in the constructor if it is needed everytime a set is made.
    }


}
